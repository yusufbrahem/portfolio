/**
 * Integration test setup. Loads env so Prisma (DATABASE_URL) and other config are available.
 * Use .env or .env.test for integration test env (e.g. a dedicated test database).
 */
import "dotenv/config";
