/** Default UI components per default menu (Skills, Experience, etc.). Used for restore and protection. */
export const DEFAULT_MENU_COMPONENT_KEYS: Record<string, string[]> = {
  skills: ["title", "pill_list"],
  experience: ["title", "timeline"],
  projects: ["title", "card_grid"],
  about: ["title", "rich_text"],
  architecture: ["title", "pillar_card"],
  contact: ["contact_block"],
};
